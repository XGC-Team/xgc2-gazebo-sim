#!/usr/bin/env bash
set -euo pipefail

requires_version_bump=false
while IFS= read -r path; do
  case "${path}" in
    .github/workflows/*|.githooks/*|agilex|camera|cerlab-uav-sim|fs150-sitl|mecanum|px4-sitl-112|px4-sitl-114|scenes|visualization|vrpn_bridge)
      ;;
    *)
      requires_version_bump=true
      ;;
  esac
done < <(git diff --cached --name-only)

if [[ "${requires_version_bump}" == "true" ]]; then
  .xgc2/scripts/check_version_bump.sh --staged
fi
